#!/usr/bin/python3 -u
# GENOCLAIM - does not accept jurisdiction !
#
#

from ok.krn import get_kernel
from ok.shl import execute, check

def main():
    check("genoclaim")
    k = get_kernel()
    k.scan("bot,genoclaim")
    k.start()
    k.init("bot.rss")
    k.wait()
    
execute(main)
