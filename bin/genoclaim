#!/usr/bin/python3 -u
# GENOCLAIM - does not accept jurisdiction !
#
# 

import ok.obj, os

from bot.spc import check, execute, get_kernel

def main():
    check("genoclaim")
    k = get_kernel()
    k.scan("bot")
    k.start()
    k.init("bot.irc,ok.csl")
    k.wait()
       
execute(main)
