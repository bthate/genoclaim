# cfg.txt

"""
    >>> import lo
    >>> cfg = lo.Default()
    >>> cfg.server = "localhost"
    >>> cfg.channel = "#botd"
    >>> cfg.nick = "botddd"
    >>> print(cfg)
    {
        "channel": "#botd",
        "nick": "botddd",
        "server": "localhost"
    }

"""

